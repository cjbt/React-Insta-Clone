import React from 'react';
import PropTypes from 'prop-types';

const BottomContentTime = props => {
  return <div>{props.timestamp}</div>;
};

BottomContentTime.propTypes = {};

export default BottomContentTime;
